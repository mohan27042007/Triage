# Shared components

Triage uses a single vanilla HTML document and has no standalone component directory. Shared primitives are CSS classes in `frontend/styles.css`: buttons, glass panels, queue cards, form controls, the pulse rail, and approval drawer.
