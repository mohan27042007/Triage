# Extractable components

There are no standalone source components to extract. The pulse rail and panel navigation are inline single-page layout elements and should remain in context as part of the dashboard shell.
