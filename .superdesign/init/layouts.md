# Shared layout

`frontend/index.html` contains the complete layout: a fixed left pulse rail, a fixed approval ribbon, a horizontal `.panel-scroller`, four `.app-section` glass panels, and an off-canvas approval drawer. The navigation controls being redesigned are `#previous-panel` and `#next-panel`.
